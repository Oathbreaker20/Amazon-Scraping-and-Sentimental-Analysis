'use strict';

function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

var fs = require('fs');

var path = require('path');

module.exports = {
  createJenkinsfile: function createJenkinsfile(cwd, simple) {
    return _asyncToGenerator( /*#__PURE__*/regeneratorRuntime.mark(function _callee() {
      var simpleJenkinsfile, noopJenkinsfile, jenkinsfilePath;
      return regeneratorRuntime.wrap(function _callee$(_context) {
        while (1) {
          switch (_context.prev = _context.next) {
            case 0:
              simpleJenkinsfile = "pipeline {\n    agent {\n        docker {\n            image 'node:10'\n            args '-u root:root'\n            reuseNode true\n        }\n    }\n    stages {\n        stage('Tests') {\n            steps {\n                sh 'npm i'\n                sh 'npm test'\n            }\n        }\n    }\n}";
              noopJenkinsfile = "pipeline {\n    agent any\n    stages {\n        stage('No-op') {\n            steps {\n                echo 'Doing nothing..'\n            }\n        }\n    }\n}";
              jenkinsfilePath = path.join(cwd, 'Jenkinsfile');

              if (!fs.existsSync(jenkinsfilePath)) {
                _context.next = 5;
                break;
              }

              return _context.abrupt("return", false);

            case 5:
              fs.writeFileSync(jenkinsfilePath, simple ? simpleJenkinsfile : noopJenkinsfile, 'utf8');
              return _context.abrupt("return", true);

            case 7:
            case "end":
              return _context.stop();
          }
        }
      }, _callee);
    }))();
  }
};