'use strict';

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); return Constructor; }

var NormalConfigStrategy = /*#__PURE__*/function () {
  function NormalConfigStrategy(context, result) {
    _classCallCheck(this, NormalConfigStrategy);

    this.context = context;
    this.result = result;
  }

  _createClass(NormalConfigStrategy, [{
    key: "setValue",
    value: function setValue(name, value) {
      this.context.mdbConfig.setValue(name, value);
      this.context.mdbConfig.save();
    }
  }, {
    key: "unsetValue",
    value: function unsetValue(name) {
      this.context.mdbConfig.unsetValue(name);
      this.context.mdbConfig.save();
    }
  }]);

  return NormalConfigStrategy;
}();

module.exports = NormalConfigStrategy;